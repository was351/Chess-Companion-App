# Hardware simulation utilities

Standalone Python helpers for hall-effect / magnet prototyping (not part of Board-Backend or firmware builds).

```bash
cd tools/hardware-sim
pip install -r requirements.txt
python hall_effect_simulator.py
# or
python magnet_visualizer.py
```
